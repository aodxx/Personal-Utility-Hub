=== specs ===
tests/e2e/community-mapping.spec.ts:6:  await page.goto('./#/tools/community-mapping');
tests/e2e/community-mapping.spec.ts:15:  await page.goto('./#/tools/community-mapping');
tests/e2e/community-mapping.spec.ts:39:  await page.goto('./#/tools/community-mapping');
tests/e2e/community-mapping.spec.ts:48:  await page.goto('./#/tools/community-mapping');
tests/e2e/community-mapping.spec.ts:62:  await page.goto('./#/tools/community-mapping');
tests/e2e/land-measurement.spec.ts:5:    await page.goto('./#/tools/land-measurement');
tests/e2e/land-measurement.spec.ts:26:    expect((await download).suggestedFilename()).toMatch(/land-measurement-csv\.csv$/);
tests/e2e/land-measurement.spec.ts:33:    await page.goto('./#/tools/land-measurement');
tests/e2e/land-measurement.spec.ts:43:    await page.goto('./#/tools/land-measurement');
tests/e2e/line-sticker-studio.spec.ts:13:    await page.goto('./#/tools/line-sticker-studio');
tests/e2e/line-sticker-studio.spec.ts:56:    await page.goto('./#/tools/line-sticker-studio');
tests/e2e/line-sticker-studio.spec.ts:72:    await page.goto('./#/tools/line-sticker-studio');
tests/e2e/line-sticker-studio.spec.ts:86:    await page.goto('./#/tools/line-sticker-studio');
tests/e2e/line-sticker-studio.spec.ts:104:    await page.goto('./#/tools/line-sticker-studio');
tests/e2e/line-sticker-studio.spec.ts:126:    await page.goto('./#/tools/line-sticker-studio');
tests/e2e/line-sticker-studio.spec.ts:144:    await page.goto('./#/tools/line-sticker-studio');
tests/e2e/line-sticker-studio.spec.ts:150:    await page.goto('./#/tools/line-sticker-studio');
tests/e2e/phase71-carousel.spec.ts:33:      'community-mapping': 20,
tests/e2e/phase71-carousel.spec.ts:40:  await expect(cards.first()).toHaveAttribute('data-tool-id', 'community-mapping');
tests/e2e/phase71-carousel.spec.ts:73:      'line-sticker-studio': 25,
tests/e2e/phase71-carousel.spec.ts:81:  await expect(cards.first()).toHaveAttribute('data-tool-id', 'line-sticker-studio');
tests/accessibility-landmarks.test.ts:6:    for (const path of ['src/tools/community-mapping/index.ts', 'src/tools/svg-asset-studio/index.ts']) {
tests/land-measurement.test.ts:2:import { buildCSV, buildGeoJSON, buildKML } from '../src/tools/land-measurement/exporters';
tests/land-measurement.test.ts:3:import { getSegments, polygonAreaSquareMeters, totalDistance, type GeoPoint } from '../src/tools/land-measurement/geometry';
tests/land-measurement.test.ts:4:import { formatThaiLandUnits, toThaiLandUnits } from '../src/tools/land-measurement/units';
tests/land-measurement.test.ts:44:    const { accuracyLevel, measurementQuality } = await import('../src/tools/land-measurement/quality');
tests/land-measurement.test.ts:52:    const { validatePolygon } = await import('../src/tools/land-measurement/quality');
tests/line-sticker-studio.test.ts:3:import { buildPrompt, createGrid, createZip, getGridCells, gridCells, gridCountMessage, moveBoundary, normalizedToSource, sourceToNormalized, validateAnimatedFrames, validateGrid } from '../src/tools/line-sticker-studio/logic';
tests/phase7-home.test.ts:33:  const tools = [tool('image-compressor'), tool('pdf-merge'), tool('qr-generator'), tool('json-formatter'), tool('audio-trimmer'), tool('line-sticker-studio', 'beta'), tool('planned', 'planned')];
tests/phase7-home.test.ts:37:    const result = mostUsedTools(tools, { 'line-sticker-studio': 12, 'audio-trimmer': 2, 'pdf-merge': 8, 'json-formatter': 4, planned: 99 }, ['image-compressor', 'pdf-merge', 'qr-generator', 'json-formatter', 'audio-trimmer'], order);
tests/phase7-home.test.ts:38:    expect(result.map(({ id }) => id)).toEqual(['line-sticker-studio', 'pdf-merge', 'json-formatter', 'audio-trimmer', 'image-compressor']);
tests/phase7-home.test.ts:44:    const result = mostUsedTools(tools, { 'line-sticker-studio': 7, 'qr-generator': 2 }, [], order);
tests/phase7-home.test.ts:45:    expect(result[0]?.id).toBe('line-sticker-studio');
tests/phase7-home.test.ts:53:    preferences.recordUse('line-sticker-studio');
tests/phase7-home.test.ts:54:    preferences.recordUse('line-sticker-studio');
tests/phase7-home.test.ts:56:    expect(preferences.getUsage()).toEqual({ 'line-sticker-studio': 2, 'qr-generator': 1 });
tests/phase7-home.test.ts:58:    expect(result[0]?.id).toBe('line-sticker-studio');
tests/search.test.ts:13:      'image-resizer', 'image-converter', 'qr-reader', 'image-compressor', 'image-blur', 'images-to-pdf', 'file-metadata', 'image-contact-sheet', 'image-watermark', 'image-crop', 'line-sticker-studio', 'svg-asset-studio',
=== community source markers ===
8:import { download, encryptBackup, exportBackup, exportCSV, exportGeoJSON, exportKML, isBackup } from './export';
25:  private onlineBasemapEnabled = false;
26:  private status = '';
41:    this.status = message;
42:    const output = this.container.querySelector<HTMLOutputElement>('[data-map-status]');
44:    const mode = this.container.querySelector<HTMLElement>('[data-mode-status]');
52:    this.container.innerHTML = `<section class="community-map-tool" aria-labelledby="community-map-title">
55:        <div class="community-map-header-actions"><span class="community-map-save-state" data-map-status aria-live="polite">${escapeHtml(this.status || 'ข้อมูลอยู่ในเครื่องนี้เท่านั้น · Local only')}</span><button class="button button--secondary" data-action="new-project">สร้าง project ใหม่</button></div>
57:      <div class="community-map-privacy"><strong>Privacy Canvas</strong><span>ไม่เรียก tile และไม่ส่ง records ออกนอกเครื่องจนกว่าคุณจะเปิด Online Basemap</span><span class="community-map-mode-pill">${this.onlineBasemapEnabled ? 'Online Basemap เปิดอยู่' : 'Offline Canvas'}</span></div>
62:            <button type="button" class="button button--secondary community-map-command is-active" data-tab="Map">🧭<span>ดูแผนที่</span></button>
67:          <div class="community-map-quick-actions"><button class="button button--secondary" data-action="import-trigger">📥 นำเข้า JSON</button><input type="file" accept="application/json,.json" data-import-file hidden><button class="button button--secondary" data-action="backup">💾 บันทึก</button><button class="button button--secondary" data-action="clear">🗑 ล้างข้อมูล</button></div>
70:          <nav class="community-map-tabs" aria-label="Community Mapping sections">${tabNames.map((tab) => `<button type="button" class="button button--secondary ${this.activeTab === tab ? 'is-active' : ''}" data-tab="${tab}">${tab === 'Map' ? 'แผนที่' : tab === 'Add' ? 'เพิ่มข้อมูล' : tab === 'Layers' ? 'Layers' : tab === 'Records' ? 'Records' : 'วิเคราะห์'}</button>`).join('')}</nav>
72:        <section class="community-map-main" role="region" aria-label="Community map workspace"><div class="community-map-map-toolbar"><div><strong>แผนที่ชุมชน</strong><span data-mode-status>เลือกโหมดเพื่อเริ่มสำรวจ</span></div><div class="community-map-map-actions"><button class="button button--secondary" data-action="toggle-labels">👁️ ${this.labelsVisible ? 'ปิด' : 'เปิด'}ป้ายชื่อ</button><button class="button button--secondary" data-action="locate">🧭 หาพิกัดฉัน</button><button class="button" data-action="online">${this.onlineBasemapEnabled ? 'ปิด Online Basemap' : 'เปิด Online Basemap'}</button></div></div><div class="community-map-canvas" data-map-host aria-label="Community map canvas"></div><div class="community-map-map-footer"><span>${this.onlineBasemapEnabled ? 'Online tiles อาจเห็น viewport ของคุณ' : 'Privacy Canvas · ไม่มี external tile requests'}</span><button class="button button--secondary" data-action="finish">Finish</button><button class="button button--secondary" data-action="cancel">Cancel</button></div></section>
74:      <div class="community-map-mobile-actions"><button class="button" data-draw="Point">📌 วางจุด</button><button class="button" data-draw="Polygon">⬡ วาดพื้นที่</button><button class="button" data-draw="LineString">〰 วาดเส้น</button><button class="button button--secondary" data-tab="Analyze">วิเคราะห์</button></div>
83:    if (tab === 'Map') return `<section class="community-map-panel" data-panel="Map"${hidden}><h3>เริ่มสำรวจพื้นที่</h3><p>เลือกโหมดจากแผงด้านข้างหรือปุ่มลอยบนมือถือ แล้วแตะบนแผนที่เพื่อสร้างข้อมูล</p><div class="community-map-guide"><span><b>01</b> กำหนดอาณาเขต</span><span><b>02</b> ปักหมุด/วาดเส้น</span><span><b>03</b> วิเคราะห์และส่งออก</span></div><div class="community-map-export-actions"><button class="button button--secondary" data-action="geojson">GeoJSON</button><button class="button button--secondary" data-action="kml">KML</button><button class="button button--secondary" data-action="csv">CSV</button><button class="button button--secondary" data-action="encrypted">Encrypted backup</button></div></section>`;
84:    if (tab === 'Add') return `<section class="community-map-panel" data-panel="Add"${hidden}><h3>เพิ่มข้อมูลภาคสนาม</h3><p>การวาดจะถูกบันทึกใน IndexedDB หลังจากสร้าง geometry สำเร็จ</p><div class="community-map-draw"><button class="button" data-draw="Point">เพิ่ม Point</button><button class="button" data-draw="LineString">วาดเส้น</button><button class="button" data-draw="Polygon">วาดพื้นที่</button></div><output class="community-map-help">${escapeHtml(this.status || 'เลือก layer ก่อนเริ่มงานภาคสนาม')}</output></section>`;
85:    if (tab === 'Layers') return `<section class="community-map-panel" data-panel="Layers"${hidden}><h3>Layers และ schema</h3><form data-form="layer"><input name="name" required placeholder="ชื่อ layer ใหม่" aria-label="ชื่อ layer ใหม่"><select name="geometry" aria-label="ประเภท geometry"><option>Point</option><option>LineString</option><option>Polygon</option></select><button class="button" type="submit">เพิ่ม layer</button></form><div class="community-map-list">${this.project.layers.map((layer) => `<label><input type="radio" name="active-layer" value="${layer.id}" ${layer.id === this.selectedLayerId ? 'checked' : ''}> ${escapeHtml(layer.name)} · ${layer.geometry}</label>`).join('')}</div><h3>Custom Schema Builder</h3><form data-form="field"><input name="name" required placeholder="ชื่อ field" aria-label="ชื่อ field"><select name="type" aria-label="ประเภท field"><option value="text">Text</option><option value="number">Number</option><option value="boolean">Boolean</option><option value="date">Date</option></select><button class="button" type="submit">เพิ่ม field</button></form><p>${this.project.schema.map((field) => `${escapeHtml(field.name)} · ${field.type}`).join(' · ') || 'ยังไม่มี custom fields'}</p></section>`;
86:    if (tab === 'Records') return `<section class="community-map-panel" data-panel="Records"${hidden}><h3>Records จากพื้นที่จริง</h3><p data-record-count>${this.project.records.length} records จาก ${this.project.features.length} features</p><ol data-record-list>${this.renderFeatureList()}</ol></section>`;
87:    return `<section class="community-map-panel" data-panel="Analyze"${hidden}><h3>วิเคราะห์ชุมชน</h3><div class="community-map-analysis"><h4>Point-in-Polygon</h4><p>ใช้ pure geometry ใน browser และถือ boundary เป็น inside</p><h4>Radius</h4><label>รัศมีเมตร <input type="number" value="1000" min="1" data-radius></label><button class="button" data-action="radius">คำนวณจาก Point แรก</button><output data-analysis-status></output></div></section>`;
101:    this.container.querySelectorAll<HTMLButtonElement>('[data-tab]').forEach((button) => button.addEventListener('click', () => { this.activeTab = button.dataset.tab as TabName; this.render(); }));
102:    this.container.querySelectorAll<HTMLButtonElement>('[data-draw]').forEach((button) => button.addEventListener('click', () => { this.activeTab = 'Map'; this.drawingMode = button.dataset.draw as GeometryType; this.drawing = []; this.setStatus(`กำลังวาด ${this.drawingMode} · แตะบนแผนที่แล้วกด Finish`); }));
103:    this.container.querySelector<HTMLButtonElement>('[data-action="finish"]')?.addEventListener('click', () => this.finishDrawing());
104:    this.container.querySelector<HTMLButtonElement>('[data-action="cancel"]')?.addEventListener('click', () => { this.drawing = []; this.drawingMode = undefined; this.setStatus('ยกเลิกการวาดแล้ว'); });
105:    this.container.querySelector<HTMLButtonElement>('[data-action="online"]')?.addEventListener('click', () => { this.onlineBasemapEnabled = !this.onlineBasemapEnabled; this.render(); this.setStatus(this.onlineBasemapEnabled ? 'Online Basemap เปิดแล้ว · provider อาจเห็น viewport' : 'กลับสู่ Privacy Canvas แล้ว'); });
106:    this.container.querySelector<HTMLButtonElement>('[data-action="toggle-labels"]')?.addEventListener('click', () => { this.labelsVisible = !this.labelsVisible; this.renderMapFeatures(); this.render(); });
107:    this.container.querySelector<HTMLButtonElement>('[data-action="locate"]')?.addEventListener('click', () => { navigator.geolocation?.getCurrentPosition((position) => this.map?.setView([position.coords.latitude, position.coords.longitude], 16), () => this.setStatus('ไม่สามารถอ่านพิกัดอุปกรณ์ได้')); });
108:    this.container.querySelector<HTMLButtonElement>('[data-action="import-trigger"]')?.addEventListener('click', () => this.container.querySelector<HTMLInputElement>('[data-import-file]')?.click());
109:    this.container.querySelector<HTMLInputElement>('[data-import-file]')?.addEventListener('change', async (event) => { const file = (event.target as HTMLInputElement).files?.[0]; if (!file) return; try { const parsed: unknown = JSON.parse(await file.text()); if (!isBackup(parsed)) throw new Error('ไฟล์นี้ไม่ใช่ Community Mapping backup'); this.project = parsed.project; this.selectedLayerId = this.project.layers[0]?.id ?? ''; this.autosave.schedule(this.project); this.setStatus('นำเข้า backup สำเร็จในเครื่องนี้'); this.render(); } catch (error) { this.setStatus(error instanceof Error ? error.message : 'นำเข้าไม่สำเร็จ'); } });
110:    this.container.querySelector<HTMLButtonElement>('[data-action="clear"]')?.addEventListener('click', () => { if (!window.confirm('ล้างข้อมูล project นี้จากอุปกรณ์หรือไม่?')) return; this.project = createProject(); this.selectedLayerId = this.project.layers[0]?.id ?? ''; this.autosave.schedule(this.project); this.setStatus('ล้างข้อมูลแล้ว'); this.render(); });
111:    this.container.querySelector<HTMLButtonElement>('[data-action="new-project"]')?.addEventListener('click', () => { const name = window.prompt('ชื่อ project ใหม่', 'Community Survey'); if (name?.trim()) { this.project = createProject(name.trim()); this.selectedLayerId = this.project.layers[0]?.id ?? ''; this.autosave.schedule(this.project); this.render(); } });
112:    this.container.querySelector<HTMLButtonElement>('[data-action="backup"]')?.addEventListener('click', () => { this.autosave.schedule(this.project); download('community-map.json', exportBackup(this.project), 'application/json'); this.setStatus('ดาวน์โหลด backup แล้ว'); });
113:    this.container.querySelector<HTMLButtonElement>('[data-action="geojson"]')?.addEventListener('click', () => download('community-map.geojson', exportGeoJSON(this.project), 'application/geo+json'));
114:    this.container.querySelector<HTMLButtonElement>('[data-action="kml"]')?.addEventListener('click', () => download('community-map.kml', exportKML(this.project), 'application/vnd.google-earth.kml+xml'));
115:    this.container.querySelector<HTMLButtonElement>('[data-action="csv"]')?.addEventListener('click', () => download('community-map.csv', exportCSV(this.project), 'text/csv'));
116:    this.container.querySelector<HTMLButtonElement>('[data-action="encrypted"]')?.addEventListener('click', async () => { const password = window.prompt('ตั้งรหัสผ่านอย่างน้อย 8 ตัวอักษร'); if (!password) return; try { download('community-map.encrypted.json', await encryptBackup(this.project, password), 'application/json'); this.setStatus('สร้าง encrypted backup แบบ local แล้ว'); } catch (error) { this.setStatus(error instanceof Error ? error.message : 'สร้าง encrypted backup ไม่สำเร็จ'); } });
117:    this.container.querySelector<HTMLInputElement>('[data-search]')?.addEventListener('input', () => this.refreshRecordList());
118:    this.container.querySelector<HTMLSelectElement>('[data-filter-layer]')?.addEventListener('change', () => this.refreshRecordList());
119:    this.container.querySelector<HTMLSelectElement>('[data-filter-geometry]')?.addEventListener('change', () => this.refreshRecordList());
120:    this.container.querySelector<HTMLButtonElement>('[data-action="radius"]')?.addEventListener('click', () => { const first = this.project.features.find((feature) => feature.geometry.type === 'Point'); const center = first?.geometry.type === 'Point' ? first.geometry.coordinates : undefined; const radius = Number(this.container.querySelector<HTMLInputElement>('[data-radius]')?.value ?? 1000); const count = center ? pointsWithinRadius(this.project.features, center, radius).length : 0; const output = this.container.querySelector<HTMLOutputElement>('[data-analysis-status]'); if (output) output.textContent = `พบ ${count} features ในรัศมี ${radius} m`; });
121:    this.container.querySelectorAll<HTMLInputElement>('[name="active-layer"]').forEach((input) => input.addEventListener('change', () => { this.selectedLayerId = input.value; }));
122:    this.container.querySelector<HTMLFormElement>('[data-form="layer"]')?.addEventListener('submit', (event) => { event.preventDefault(); const form = new FormData(event.currentTarget as HTMLFormElement); const name = String(form.get('name') ?? '').trim(); if (!name) return; this.project.layers.push({ id: uid('layer'), name, geometry: String(form.get('geometry')) as GeometryType, color: '#2563eb', visible: true }); this.selectedLayerId = this.project.layers.at(-1)?.id ?? this.selectedLayerId; this.autosave.schedule(this.project); this.render(); });
123:    this.container.querySelector<HTMLFormElement>('[data-form="field"]')?.addEventListener('submit', (event) => { event.preventDefault(); const form = new FormData(event.currentTarget as HTMLFormElement); const name = String(form.get('name') ?? '').trim(); if (!name) return; this.project.schema.push({ id: uid('field'), name, type: String(form.get('type')) as 'text' | 'number' | 'boolean' | 'date', required: false }); this.autosave.schedule(this.project); this.render(); });
126:  private refreshRecordList(): void { const list = this.container.querySelector<HTMLOListElement>('[data-record-list]'); if (list) list.innerHTML = this.renderFeatureList(); const count = this.container.querySelector<HTMLElement>('[data-record-count]'); if (count) count.textContent = `${this.project.records.length} records จาก ${this.project.features.length} features`; }
128:  private initMap(): void { const host = this.container.querySelector<HTMLElement>('[data-map-host]'); if (!host) return; this.map = L.map(host, { zoomControl: true }).setView([13.7563, 100.5018], 13); this.createPrivacyBasemap().addTo(this.map); this.layerGroup = L.layerGroup().addTo(this.map); if (this.onlineBasemapEnabled) this.enableBasemap(); this.map.on('click', (event) => { if (!this.drawingMode) return; this.drawing.push(event.latlng); if (this.drawingMode === 'Point') this.finishDrawing(); else L.circleMarker(event.latlng, { radius: 5, color: '#2563eb' }).addTo(this.layerGroup!); }); this.renderMapFeatures(); }
135:        tile.innerHTML = `<svg viewBox="0 0 256 256" role="presentation" aria-hidden="true"><rect width="256" height="256" fill="#edf3e8"/><path d="M-20 46 C45 18 74 78 126 56 S208 22 280 52" fill="none" stroke="#d5e3cc" stroke-width="18"/><path d="M-20 46 C45 18 74 78 126 56 S208 22 280 52" fill="none" stroke="#fff" stroke-width="2"/><path d="M-28 208 C20 176 70 214 112 182 S204 176 278 208" fill="none" stroke="#d7e1ef" stroke-width="24"/><path d="M-28 208 C20 176 70 214 112 182 S204 176 278 208" fill="none" stroke="#9ec5e6" stroke-width="3"/><g fill="none" stroke="#d0d7c8" stroke-width="3"><path d="M12 0 L68 256"/><path d="M92 0 L142 256"/><path d="M198 0 L176 256"/><path d="M0 112 L256 84"/><path d="M0 154 L256 132"/></g><g fill="#87947f" font-family="sans-serif" font-size="10"><text x="18" y="28">ชุมชน</text><text x="164" y="116">พื้นที่สำรวจ</text><text x="42" y="226">Privacy Canvas</text></g></svg>`;
149:export { metadata };
150:export const mount = async (container: HTMLElement): Promise<void> => { const controller = new CommunityMappingController(container); activeUnmount = () => controller.unmount(); await controller.mount(); };
151:export const unmount = (): void => activeUnmount?.();
152:export default { metadata, mount, unmount } satisfies ToolModule;
=== land source markers ===
3:import { downloadUrl, setToolStatus } from '../../core/tool-ui';
5:import { buildCSV, buildGeoJSON, buildKML } from './exporters';
36:    this.container.innerHTML = `<section class="utility-panel land-measurement" aria-labelledby="land-title"><header class="land-header"><div><p class="eyebrow">Local map measurement / วัดจาก browser</p><h2 id="land-title">วัดระยะและพื้นที่แปลง</h2><p class="helper-text">แตะจุดบนแผนที่เพื่อวัดระยะ เส้นรอบรูป หรือพื้นที่แปลง</p></div><span class="privacy-badge">Local geometry / ไม่อัปโหลดพิกัด</span></header><div class="land-disclaimer" role="note">ผลการวัดเป็นค่าประมาณจากพิกัดและแผนที่ ไม่ใช้แทนการรังวัดโดยเจ้าหน้าที่หรือเครื่องมือสำรวจมาตรฐาน</div><div class="land-toolbar" role="toolbar" aria-label="Measurement controls"><div class="land-mode-buttons"><button type="button" class="button is-active" data-mode="distance">วัดระยะ</button><button type="button" class="button" data-mode="area">วัดพื้นที่</button><button type="button" class="button" data-mode="gps">เดินวัด GPS</button></div><div class="land-map-buttons"><button type="button" class="button button--secondary" data-action="locate">ตำแหน่งของฉัน</button><button type="button" class="button button--secondary" data-action="fit">จัดแผนที่ให้เห็นจุด</button><button type="button" class="button button--secondary" data-action="layer">แผนที่ / ดาวเทียม</button></div></div><div class="land-layout"><div class="land-map-wrap"><div id="land-map" class="land-map" aria-label="Interactive measurement map"></div><p id="land-status" class="tool-status" aria-live="polite">เลือกโหมด แล้วแตะบนแผนที่เพื่อเพิ่มจุด</p><div class="land-map-actions"><button type="button" class="button button--secondary" data-action="undo">ย้อนจุดล่าสุด</button><button type="button" class="button button--secondary" data-action="clear">ล้างทั้งหมด</button></div></div><aside class="land-results" aria-labelledby="land-results-title"><h3 id="land-results-title">ผลการวัด / Results</h3><div id="land-summary" class="land-summary"></div><div id="land-points" class="land-points"></div><div class="land-exports"><strong>ส่งออก / Export</strong><div><button type="button" class="button" data-export="geojson">GeoJSON</button><button type="button" class="button" data-export="kml">KML</button><button type="button" class="button" data-export="csv">CSV</button><button type="button" class="button button--primary" data-action="copy">คัดลอกผลลัพธ์</button></div></div><p class="helper-text">แผนที่มาตรฐานโหลดจาก OpenStreetMap เมื่อออนไลน์; โหมดดาวเทียมใช้ Esri tiles และอาจมีเงื่อนไขผู้ให้บริการ</p></aside></div></section>`;
44:    this.basemap = L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', { attribution: '&copy; OpenStreetMap contributors', maxZoom: 19 }).addTo(this.map); this.basemap.on('tileerror', () => this.useFallbackBasemap());
50:    this.container.querySelectorAll<HTMLButtonElement>('[data-mode]').forEach((button) => button.addEventListener('click', () => {
52:      this.container.querySelectorAll('[data-mode]').forEach((node) => node.classList.toggle('is-active', node === button));
56:    this.container.querySelector<HTMLButtonElement>('[data-action="layer"]')?.addEventListener('click', () => this.toggleLayer());
57:    this.container.querySelector<HTMLButtonElement>('[data-action="locate"]')?.addEventListener('click', () => this.locate());
58:    this.container.querySelector<HTMLButtonElement>('[data-action="fit"]')?.addEventListener('click', () => this.fitMap());
59:    this.container.querySelector<HTMLButtonElement>('[data-action="undo"]')?.addEventListener('click', () => { this.points.pop(); this.render(); });
60:    this.container.querySelector<HTMLButtonElement>('[data-action="clear"]')?.addEventListener('click', () => { this.points = []; this.render(); this.setStatus('ล้างข้อมูลการวัดแล้ว / Measurement cleared', 'success'); });
61:    this.container.querySelector<HTMLButtonElement>('[data-action="copy"]')?.addEventListener('click', () => void this.copySummary());
62:    this.container.querySelectorAll<HTMLButtonElement>('[data-export]').forEach((button) => button.addEventListener('click', () => this.export(button.dataset.export as ExportFormat)));
64:    document.addEventListener('keydown', this.onKeyDown);
77:      if (!response.ok) throw new Error(`offline roads ${response.status}`);
81:      this.setStatus('ข้อมูลถนนออฟไลน์พัทลุงโหลดไม่สำเร็จ / Phatthalung offline roads unavailable', 'error');
94:    this.offlineRoadLayer.bringToFront(); this.map.attributionControl.addAttribution('&copy; OpenStreetMap contributors · HOT/HDX road export'); this.container.querySelector<HTMLElement>('#land-map')?.setAttribute('data-offline-roads', 'ready');
101:      if (!response.ok) throw new Error(`offline basemap ${response.status}`);
106:      this.setStatus('แผนที่ขอบเขตออฟไลน์โหลดไม่สำเร็จ / Offline boundary map unavailable', 'error');
115:    this.basemap.on('tileerror', () => this.showLocalFallback());
120:  private showLocalFallback(): void { if (!this.map || this.localFallback) return; const host = this.container.querySelector<HTMLElement>('#land-map'); if (!host) return; const fallback = document.createElement('div'); fallback.className = 'local-map-fallback'; fallback.innerHTML = '<strong>OFFLINE STREET MAP / ถนนออฟไลน์</strong><span>Phatthalung roads · ถนนพัทลุง · local measurement remains available</span>'; host.append(fallback); this.localFallback = fallback; void this.loadLocalBasemap(); void this.loadOfflineRoads(); if (this.points.length) this.fitMap(); else this.map.setView([7.6167, 100.074], 11); this.setStatus('ไม่พบภาพแผนที่ออนไลน์ แสดงถนนพัทลุงแบบออฟไลน์ / Online tiles unavailable; Phatthalung offline street map is active', 'working'); }
133:    this.container.querySelector<HTMLButtonElement>('[data-action="capture"]')?.remove();
134:    if (this.mode === 'gps') { const button = document.createElement('button'); button.type = 'button'; button.className = 'button button--primary land-gps-capture'; button.dataset.action = 'capture'; button.textContent = 'บันทึกจุด GPS / Capture GPS point'; button.addEventListener('click', () => this.captureGps()); this.container.querySelector('.land-map-buttons')?.append(button); }
143:    summary.innerHTML = `<div class="land-quality ${qualityClass}" role="status"><span>คุณภาพข้อมูล / Data quality</span><strong>${measurementQualityLabel(quality)}</strong></div><div class="land-metric"><span>${this.mode === 'area' ? 'พื้นที่ / Area' : 'ระยะรวม / Total distance'}</span><strong>${this.mode === 'area' ? (validation.valid ? formatArea(area) : 'ตรวจจุดก่อนคำนวณ / Fix points first') : formatMeters(perimeter)}</strong></div>${this.mode === 'area' && validation.valid ? `<div class="land-metric"><span>เส้นรอบรูป / Perimeter</span><strong>${formatMeters(perimeter)}</strong></div><div class="land-metric"><span>ศูนย์กลางโดยประมาณ / Centroid</span><strong>${centroid ? `${centroid.lat.toFixed(6)}, ${centroid.lng.toFixed(6)}` : '-'}</strong></div>` : ''}${validation.issues.length ? `<div class="land-validation" role="alert"><strong>ตรวจสอบ geometry / Geometry check</strong><ul>${validation.issues.map((issue) => `<li>${issue}</li>`).join('')}</ul></div>` : ''}<div class="land-segments"><strong>ระยะรายด้าน / Segments</strong>${segments.length ? `<ol>${segments.map((segment) => `<li>P${segment.from + 1} → P${segment.to + 1}: ${formatMeters(segment.meters)}</li>`).join('')}</ol>` : '<p>เพิ่มอย่างน้อย 2 จุด / Add at least two points</p>'}</div>`;
144:    points.innerHTML = this.points.length ? `<h4>จุดที่ปัก / Points (${this.points.length})</h4><ol>${this.points.map((point, index) => { const level = accuracyLevel(point.accuracyMeters); const recapture = point.source === 'gps' ? `<button type="button" class="button button--secondary land-point-recapture" data-recapture="${point.id}">อ่านใหม่ / Recapture</button>` : ''; return `<li><div class="land-point-row"><button type="button" class="land-point-delete" data-delete="${point.id}" aria-label="ลบจุดที่ ${index + 1}">×</button><span>${index + 1}. ${point.lat.toFixed(6)}, ${point.lng.toFixed(6)} <small>${point.source} · ${point.accuracyMeters !== undefined ? `±${Math.round(point.accuracyMeters)} m · ${accuracyLabel(level)}` : 'accuracy unknown / ไม่ทราบ accuracy'}</small></span>${recapture}</div></li>`; }).join('')}</ol>` : '<p>ยังไม่มีจุด / No points yet</p>';
145:    points.querySelectorAll<HTMLButtonElement>('[data-delete]').forEach((button) => button.addEventListener('click', () => { this.points = this.points.filter((point) => point.id !== button.dataset.delete); this.render(); }));
146:    points.querySelectorAll<HTMLButtonElement>('[data-recapture]').forEach((button) => button.addEventListener('click', () => this.captureGps(button.dataset.recapture)));
150:  private toggleLayer(): void { if (!this.map) return; if (this.layer === 'standard') { this.basemap?.remove(); this.satellite = L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}', { attribution: 'Tiles &copy; Esri', maxZoom: 19 }).addTo(this.map); this.satellite.on('tileerror', () => this.showLocalFallback()); this.satellite.once('tileload', () => this.localFallback?.remove()); this.layer = 'satellite'; } else { this.satellite?.remove(); this.basemap?.addTo(this.map); this.layer = 'standard'; } this.setStatus(this.layer === 'satellite' ? 'เปิดภาพดาวเทียมแล้ว / Satellite layer on' : 'เปิดแผนที่มาตรฐานแล้ว / Standard map on', 'success'); }
151:  private locate(): void { if (!navigator.geolocation) { this.setStatus('เบราว์เซอร์ไม่รองรับ GPS / Geolocation is unavailable', 'error'); return; } this.setStatus('กำลังค้นหาตำแหน่ง / Locating…', 'working'); navigator.geolocation.getCurrentPosition((position) => { const { latitude, longitude, accuracy } = position.coords; this.map?.setView([latitude, longitude], 17); this.accuracyCircle?.remove(); this.accuracyCircle = L.circle([latitude, longitude], { radius: accuracy, color: '#f59e0b', fillOpacity: 0.12 }).addTo(this.map!); this.setStatus(`ตำแหน่งโดยประมาณ ±${Math.round(accuracy)} m / Location accuracy ±${Math.round(accuracy)} m`, 'success'); }, (error) => this.setStatus(error.code === 1 ? 'ไม่ได้รับสิทธิ์ตำแหน่ง / Location permission denied' : 'ค้นหาตำแหน่งไม่สำเร็จ / Location unavailable', 'error'), { enableHighAccuracy: true, timeout: 12_000, maximumAge: 10_000 }); }
152:  private captureGps(replaceId?: string): void { if (!navigator.geolocation) { this.setStatus('เบราว์เซอร์ไม่รองรับ GPS / Geolocation is unavailable', 'error'); return; } this.setStatus('กำลังอ่าน GPS / Reading GPS…', 'working'); navigator.geolocation.getCurrentPosition((position) => this.addPoint(position.coords.latitude, position.coords.longitude, 'gps', position.coords.accuracy, replaceId), () => this.setStatus('อ่าน GPS ไม่สำเร็จ กรุณาตรวจ permission และสัญญาณ / GPS failed', 'error'), { enableHighAccuracy: true, timeout: 12_000, maximumAge: 0 }); }
154:  private async copySummary(): Promise<void> { const text = this.summaryText(); try { if (navigator.clipboard) await navigator.clipboard.writeText(text); else { const textarea = document.createElement('textarea'); textarea.value = text; textarea.style.position = 'fixed'; textarea.style.opacity = '0'; document.body.append(textarea); textarea.select(); if (!document.execCommand('copy')) throw new Error('Copy failed'); textarea.remove(); } this.setStatus('คัดลอกผลการวัดแล้ว / Summary copied', 'success'); } catch { this.setStatus('คัดลอกไม่สำเร็จ / Copy failed', 'error'); } }
155:  private export(format: ExportFormat): void { if (!this.points.length) { this.setStatus('เพิ่มจุดก่อนส่งออก / Add points before export', 'error'); return; } const content = format === 'geojson' ? buildGeoJSON(this.points, this.mode) : format === 'kml' ? buildKML(this.points, this.mode) : buildCSV(this.points); const mime = format === 'geojson' ? 'application/geo+json' : format === 'kml' ? 'application/vnd.google-earth.kml+xml' : 'text/csv;charset=utf-8'; const url = URL.createObjectURL(new Blob([content], { type: mime })); downloadUrl(url, `land-measurement-${format}.${format === 'geojson' ? 'geojson' : format}`); window.setTimeout(() => URL.revokeObjectURL(url), 1000); this.setStatus(`${format.toUpperCase()} สร้างแล้ว / Export created`, 'success'); }
156:  private setStatus(message: string, tone: 'neutral' | 'working' | 'success' | 'error' = 'neutral'): void { const output = this.container.querySelector<HTMLOutputElement>('#land-status'); if (output) setToolStatus(output, message, tone); }
157:  unmount(): void { if (this.onKeyDown) document.removeEventListener('keydown', this.onKeyDown); this.accuracyCircle?.remove(); this.localFallback?.remove(); this.offlineRoadLayer?.remove(); this.localBasemap?.remove(); this.map?.remove(); this.map = undefined; this.points = []; this.markers.clear(); this.container.innerHTML = ''; }
160:export const mount = async (container: HTMLElement): Promise<void> => { const controller = new LandMeasurementController(container); activeUnmount = () => controller.unmount(); controller.mount(); };
161:export const unmount = (): void => { activeUnmount?.(); activeUnmount = undefined; };
162:export { metadata };
163:export default { metadata, mount, unmount } satisfies ToolModule;
=== line source markers ===
2:import { downloadUrl, formatBytes, getErrorMessage, setProgressStatus, setToolStatus } from '../../core/tool-ui';
4:import { ANIMATED_STICKER_PRESET, CHAT_THUMBNAIL_PRESET, MAIN_IMAGE_PRESET, STATIC_STICKER_PRESET } from '../../data/line-sticker-presets';
8:type SplitState = 'empty' | 'source-loaded' | 'grid-ready' | 'split-complete' | 'processing' | 'review' | 'export-ready';
28:let currentStep = 'prompt';
66:function performSplit(): void { const source = sourceCanvas; if (!source || !grid) { updateStatus('ต้องอัปโหลด Sticker Sheet และเลือก Grid ก่อน / Upload a sheet and choose a grid first', 'error'); return; } const validation = validateGrid(grid, source.width, source.height); if (!validation.valid) { splitState = 'grid-ready'; updateStatus(`Grid ไม่ถูกต้อง กรุณาปรับเส้นตัดก่อน / ${validation.errors.join('; ')}`, 'error'); return; } const next: StickerItem[] = []; try { const cells = getGridCells(grid, source.width, source.height); cells.forEach((cell, index) => { if (cell.x < 0 || cell.y < 0 || cell.width < 1 || cell.height < 1 || cell.x + cell.width > source.width || cell.y + cell.height > source.height) throw new Error(`Cell ${index + 1} crop bounds are invalid`); const canvas = createCanvas(cell.width, cell.height); const context = canvas.getContext('2d'); if (!context) throw new Error('Canvas ไม่พร้อมใช้งาน'); context.drawImage(source, cell.x, cell.y, cell.width, cell.height, 0, 0, cell.width, cell.height); if (canvas.width !== cell.width || canvas.height !== cell.height) throw new Error(`Cell ${index + 1} output dimensions are invalid`); next.push({ id: `${Date.now()}-${index}`, name: `sticker-${String(index + 1).padStart(2, '0')}`, canvas, originalCanvas: cloneCanvas(canvas), transform: defaultTransform(), style: defaultStyle(), history: [] }); }); } catch (error) { next.forEach(cleanupSticker); splitState = 'grid-ready'; updateStatus(getErrorMessage(error), 'error'); return; } stickers.forEach(cleanupSticker); stickers = next; selectedIndex = 0; splitState = 'split-complete'; currentStep = 'edit'; renderGridOverlay(); renderWorkspace(); updateStatus(`${stickers.length} stickers split / แยก ${stickers.length} stickers แล้ว`, 'success'); }
71:  const summary = createValidationSummary(results); report.innerHTML = `<strong>${summary.pass} PASS · ${summary.warning} WARNING · ${summary.fail} FAIL</strong><div>${summary.issues.map((issue) => `<p><b>${issue.level}</b> Sticker ${issue.sticker}: ${issue.detail} — ${issue.fix}</p>`).join('')}</div>`;
79:  const sourcePreview = panel?.querySelector<HTMLCanvasElement>('#line-source-preview'); if (sourcePreview) { sourcePreview.width = source.width; sourcePreview.height = source.height; sourcePreview.getContext('2d')?.drawImage(source, 0, 0); }
80:  const sourceWidth = source.width; const sourceHeight = source.height; let cells; try { cells = getGridCells(grid, sourceWidth, sourceHeight); } catch (error) { overlay.innerHTML = `<p class="line-grid-error">${getErrorMessage(error)}</p>`; return; } overlay.innerHTML = ''; cells.forEach((cell, cellIndex) => { const marker = document.createElement('span'); marker.className = 'line-grid-cell-number'; marker.textContent = String(cellIndex + 1).padStart(2, '0'); marker.style.left = `${(cell.x / sourceWidth) * 100}%`; marker.style.top = `${(cell.y / sourceHeight) * 100}%`; marker.style.width = `${(cell.width / sourceWidth) * 100}%`; marker.style.height = `${(cell.height / sourceHeight) * 100}%`; overlay.append(marker); });
84:    input.addEventListener('input', () => { if (!grid) return; const target = line.axis === 'x' ? grid.xBoundaries : grid.yBoundaries; const boundaryIndex = Number(input.dataset.boundary); const next = moveBoundary(target, boundaryIndex + 1, Number(input.value)); if (line.axis === 'x') grid.xBoundaries = next; else grid.yBoundaries = next; renderGridOverlay(); updateStatus(`${line.axis === 'x' ? 'Vertical' : 'Horizontal'} boundary adjusted`); });
106:  const rail = el<HTMLElement>('#line-thumbnails'); rail.innerHTML = '';
112:    wrapper.addEventListener('click', () => { selectedIndex = index; renderWorkspace(); }); wrapper.addEventListener('keydown', (event) => { if (event.key === 'ArrowRight' || event.key === 'ArrowDown') { selectedIndex = Math.min(stickers.length - 1, index + 1); renderWorkspace(); } if (event.key === 'ArrowLeft' || event.key === 'ArrowUp') { selectedIndex = Math.max(0, index - 1); renderWorkspace(); } }); wrapper.tabIndex = 0; wrapper.setAttribute('role', 'button'); rail.append(wrapper);
117:  const output = el<HTMLElement>('#line-inspector'); output.innerHTML = '';
120:  const summary = document.createElement('div'); summary.className = `line-inspector-summary line-inspector-summary--${item.quality.level.toLowerCase()}`; summary.innerHTML = `<strong>${item.quality.level}</strong><span>${item.quality.checks.filter((check) => check.level === 'PASS').length} PASS · ${item.quality.checks.filter((check) => check.level === 'WARNING').length} WARNING · ${item.quality.checks.filter((check) => check.level === 'FAIL').length} FAIL</span>`; output.append(summary);
121:  item.quality.checks.forEach((check) => { const row = document.createElement('div'); row.className = 'line-inspector-row'; row.innerHTML = `<b>${check.level}</b><span><strong>${check.label}</strong><small>${check.detail}<br/>${check.fix}</small></span>`; output.append(row); });
124:function canProcess(action: string): boolean { if (splitState !== 'split-complete' && splitState !== 'review' && splitState !== 'export-ready') { updateStatus(`${action} ทำได้หลังจากกด Quick Split เท่านั้น / ${action} is available after Quick Split`, 'error'); return false; } return true; }
127:  const buttons = panel?.querySelectorAll<HTMLButtonElement>('[data-line-step]'); buttons?.forEach((button) => button.classList.toggle('is-current', button.dataset.lineStep === currentStep));
128:  const ready = splitState === 'split-complete' || splitState === 'review' || splitState === 'export-ready'; ['#line-clean','#line-clean-all','#line-autofit','#line-autofit-all','#line-border-all','#line-finish-all','#line-apply-style','#line-reset-current','#line-review-refresh','#line-fix-next','#line-export-zip','#line-download-current'].forEach((selector) => { const button = panel?.querySelector<HTMLButtonElement>(selector); if (button) button.disabled = !ready; });
131:function updateStatus(message: string, tone: 'neutral' | 'working' | 'success' | 'error' = 'neutral'): void { if (panel) setToolStatus(el<HTMLOutputElement>('#line-status'), message, tone); }
136:  const status = el<HTMLOutputElement>('#line-status'); setProgressStatus(status, 10, 'กำลังอ่านภาพในอุปกรณ์ / Reading images locally');
142:    sourceCanvas = await fileToCanvas(firstFile); const preset = el<HTMLSelectElement>('#line-preset-grid')?.value; const presetRows = preset === '3x3' ? 3 : preset === '4x4' ? 4 : preset === '4x5' ? 4 : Number(el<HTMLSelectElement>('#line-rows').value); const presetColumns = preset === '3x3' ? 3 : preset === '4x4' ? 4 : preset === '4x5' ? 5 : Number(el<HTMLSelectElement>('#line-columns').value); el<HTMLSelectElement>('#line-rows').value = String(presetRows); el<HTMLSelectElement>('#line-columns').value = String(presetColumns); updateSplitSummary(); grid = createGrid(sourceCanvas.width, sourceCanvas.height, presetRows, presetColumns); splitState = 'grid-ready'; currentStep = 'upload'; updateSplitSummary();
144:  renderGridOverlay(); renderWorkspace(); setProgressStatus(status, 100, splitState === 'grid-ready' ? 'Source พร้อมแล้ว · กดยืนยัน Grid เพื่อเริ่มตัด / Source ready · confirm the grid to split' : `${stickers.length} stickers ready / พร้อม ${stickers.length} stickers`); updateStatus(splitState === 'grid-ready' ? `Source ${sourceCanvas?.width}×${sourceCanvas?.height} พร้อม · Suggested Grid ${grid?.rows}×${grid?.columns} · กดตัด ${grid ? grid.rows * grid.columns : 0} ภาพ` : `${stickers.length} stickers ready / พร้อม ${stickers.length} stickers`, 'success');
158:async function runBatch(label: string, action: (item: StickerItem) => void): Promise<void> { if (!canProcess(label)) return; if (!stickers.length) { updateStatus('ยังไม่มีภาพสำหรับจัดการ / Add stickers first', 'error'); return; } const output = el<HTMLOutputElement>('#line-batch-status'); for (const [index, item] of stickers.entries()) { action(item); output.textContent = `${label} · ${index + 1} / ${stickers.length}`; await new Promise<void>((resolve) => requestAnimationFrame(() => resolve())); } renderWorkspace(); output.textContent = `${label} เสร็จแล้ว · ${stickers.length} / ${stickers.length}`; updateStatus(`${label} เสร็จแล้ว / ${label} complete`, 'success'); }
167:async function exportSet(): Promise<void> { if (!canProcess('ดาวน์โหลด')) return; if (!stickers.length) throw new Error('Add stickers before export'); const current = stickers[selectedIndex] ?? stickers[0]; if (!current) throw new Error('Add stickers before export'); updateStatus('Encoding and validating PNG outputs / กำลัง encode และตรวจ PNG', 'working'); const files = []; for (const [index, item] of stickers.entries()) { updateStatus(`กำลังสร้าง PNG ${index + 1}/${stickers.length} / Encoding PNG ${index + 1}/${stickers.length}`, 'working'); const blob = await encodeItem(item); files.push({ name: `stickers/${String(index + 1).padStart(2, '0')}.png`, bytes: new Uint8Array(await blob.arrayBuffer()) }); await new Promise<void>((resolve) => requestAnimationFrame(() => resolve())); }
168:  const main = await createResizedBlob(current, MAIN_IMAGE_PRESET.dimensions.width, MAIN_IMAGE_PRESET.dimensions.height); const tab = await createResizedBlob(current, CHAT_THUMBNAIL_PRESET.dimensions.width, CHAT_THUMBNAIL_PRESET.dimensions.height); const summary = createValidationSummary(stickers.map((item, index) => ({ name: `${index + 1}`, quality: item.quality! }))); const report = JSON.stringify({ generatedAt: new Date().toISOString(), preset: STATIC_STICKER_PRESET.id, technicalOnly: true, summary, stickers: stickers.map((item) => ({ name: item.name, quality: item.quality })) }, null, 2); files.push({ name: 'main.png', bytes: new Uint8Array(await main.arrayBuffer()) }, { name: 'tab.png', bytes: new Uint8Array(await tab.arrayBuffer()) }, { name: 'validation-report.json', bytes: new TextEncoder().encode(report) }); const zip = createZip(files); downloadUrl(URL.createObjectURL(zip), `${sourceName}-line-stickers.zip`); const txt = `LINE Sticker Studio technical validation\nPASS ${summary.pass}\nWARNING ${summary.warning}\nFAIL ${summary.fail}\n\n${summary.issues.map((issue) => `${issue.level} ${issue.sticker}: ${issue.detail} — ${issue.fix}`).join('\n')}`; downloadUrl(URL.createObjectURL(new Blob([txt], { type: 'text/plain' })), `${sourceName}-validation.txt`); updateStatus(`ZIP ถูกสร้างแล้ว · ${formatBytes(zip.size)} · ${summary.pass} PASS / ${summary.warning} WARNING / ${summary.fail} FAIL`, summary.fail ? 'error' : 'success'); const exportSummary = panel?.querySelector<HTMLElement>('#line-export-summary'); if (exportSummary) exportSummary.innerHTML = `<strong>ZIP ถูกสร้างแล้ว</strong><span>${stickers.length} stickers · main.png · tab.png · validation-report.json · ${formatBytes(zip.size)}</span><span>${summary.pass} พร้อม · ${summary.warning} ต้องตรวจ · ${summary.fail} ผิดพลาด</span>`; renderWorkspace(); }
170:function downloadCurrent(): void { const item = stickers[selectedIndex]; if (!item?.blob) { updateStatus('Run ZIP export once to encode the current PNG / กด Export ZIP เพื่อ encode PNG ก่อน', 'error'); return; } downloadUrl(URL.createObjectURL(item.blob), `${sourceName}-${String(selectedIndex + 1).padStart(2, '0')}.png`); }
171:function resetCurrent(): void { if (!canProcess('Reset')) return; const item = stickers[selectedIndex]; if (!item?.originalCanvas) return; item.canvas.width = item.originalCanvas.width; item.canvas.height = item.originalCanvas.height; item.canvas.getContext('2d')!.drawImage(item.originalCanvas, 0, 0); item.blob = undefined; item.quality = undefined; item.history = []; renderWorkspace(); updateStatus('กู้ภาพต้นฉบับของ sticker ปัจจุบันแล้ว / Current sticker restored', 'success'); }
172:function undoCurrent(): void { const item = stickers[selectedIndex]; const state = item?.history.pop(); if (!item || !state) return; item.transform = state.transform; item.style = state.style; renderWorkspace(); updateStatus('Undo complete / ย้อนกลับแล้ว', 'success'); }
176:function createNewSet(): void { if (stickers.length || el<HTMLTextAreaElement>('#line-prompt-th')?.value || el<HTMLInputElement>('#line-prompt-character')?.value) { if (!window.confirm('มี draft หรือภาพใน workspace ต้องการสร้างชุดใหม่และล้างข้อมูลในหน้านี้หรือไม่?')) return; } stickers.forEach(cleanupSticker); sourceCanvas?.remove(); stickers = []; sourceCanvas = undefined; grid = undefined; selectedIndex = 0; currentStep = 'prompt'; localStorage.removeItem(DRAFT_KEY); const file = el<HTMLInputElement>('#line-file'); file.value = ''; el<HTMLTextAreaElement>('#line-prompt-th').value = ''; el<HTMLTextAreaElement>('#line-prompt-en').value = ''; renderGridOverlay(); renderWorkspace(); updateStatus('พร้อมสร้างชุดใหม่ / Ready for a new set', 'success'); }
182:function persistDraft(): void { if (!panel) return; const fields = ['character', 'style', 'outfit', 'expressions', 'phrases', 'language', 'count', 'background'].map((name) => [`${name}`, el<HTMLInputElement>(`#line-prompt-${name}`)?.value ?? '']); const draft = { fields: Object.fromEntries(fields), rows: el<HTMLSelectElement>('#line-rows')?.value ?? '2', columns: el<HTMLSelectElement>('#line-columns')?.value ?? '4', latestStep: currentStep, promptTh: el<HTMLTextAreaElement>('#line-prompt-th')?.value ?? '', promptEn: el<HTMLTextAreaElement>('#line-prompt-en')?.value ?? '' }; localStorage.setItem(DRAFT_KEY, JSON.stringify(draft)); }
183:function restoreDraft(): void { if (!panel) return; try { const draft = JSON.parse(localStorage.getItem(DRAFT_KEY) || 'null') as { fields?: Record<string, string>; rows?: string; columns?: string; latestStep?: string; promptTh?: string; promptEn?: string } | null; if (!draft) return; Object.entries(draft.fields ?? {}).forEach(([name, value]) => { const input = panel!.querySelector<HTMLInputElement>(`#line-prompt-${name}`); if (input && value !== undefined) input.value = value; }); if (draft.rows) el<HTMLSelectElement>('#line-rows').value = draft.rows; if (draft.columns) el<HTMLSelectElement>('#line-columns').value = draft.columns; if (draft.promptTh) el<HTMLTextAreaElement>('#line-prompt-th').value = draft.promptTh; if (draft.promptEn) el<HTMLTextAreaElement>('#line-prompt-en').value = draft.promptEn; if (draft.latestStep) currentStep = draft.latestStep; } catch { localStorage.removeItem(DRAFT_KEY); } }
184:async function copyPrompt(): Promise<boolean> { const text = `${el<HTMLTextAreaElement>('#line-prompt-th').value}\n\n${el<HTMLTextAreaElement>('#line-prompt-en').value}`.trim(); if (!text) { buildPromptUi(); } const value = `${el<HTMLTextAreaElement>('#line-prompt-th').value}\n\n${el<HTMLTextAreaElement>('#line-prompt-en').value}`.trim(); try { await navigator.clipboard.writeText(value); return true; } catch { const textarea = document.createElement('textarea'); textarea.value = value; textarea.style.position = 'fixed'; textarea.style.opacity = '0'; document.body.append(textarea); textarea.focus(); textarea.select(); const copied = document.execCommand('copy'); textarea.remove(); return copied; } }
185:function openChatGPT(): void { const opened = window.open('https://chatgpt.com/', '_blank', 'noopener,noreferrer'); if (opened) updateStatus('Prompt ถูกคัดลอกแล้ว กำลังเปิด ChatGPT / Prompt copied — opening ChatGPT', 'success'); else updateStatus('Browser ไม่อนุญาตให้เปิดแท็บใหม่ / Popup blocked. ใช้ปุ่มเปิด ChatGPT อีกครั้ง', 'error'); }
186:function buildPromptUi(): void { const input = (id: string) => (el<HTMLInputElement>(id)?.value || '').trim(); const rows = Number(el<HTMLSelectElement>('#line-rows').value || 2); const columns = Number(el<HTMLSelectElement>('#line-columns').value || 4); const count = Number(input('#line-prompt-count') || rows * columns); const phrases = input('#line-prompt-phrases'); const prompt = buildPrompt({ character: input('#line-prompt-character'), style: input('#line-prompt-style'), outfit: input('#line-prompt-outfit'), expressions: input('#line-prompt-expressions'), phrases, language: input('#line-prompt-language'), count, rows, columns, background: input('#line-prompt-background'), consistency: Array.from(panel!.querySelectorAll<HTMLInputElement>('[data-consistency]:checked')).map((node) => node.value) }); el<HTMLTextAreaElement>('#line-prompt-th').value = prompt.th; el<HTMLTextAreaElement>('#line-prompt-en').value = prompt.en; const phraseCount = phrases ? phrases.split(/[,\n]/).map((value) => value.trim()).filter(Boolean).length : 0; const warning = gridCountMessage(rows, columns, phraseCount); const notice = el<HTMLElement>('#line-grid-warning'); notice.textContent = warning.message; notice.dataset.level = warning.level; persistDraft(); updateStatus(warning.message, warning.level === 'WARNING' ? 'neutral' : 'success'); }
189:  el<HTMLInputElement>('#line-file').addEventListener('change', (event) => { const files = (event.currentTarget as HTMLInputElement).files; if (!files) return; void (mode === 'animated' ? loadAnimatedFiles(files) : loadStaticFiles(files)).catch((error) => updateStatus(getErrorMessage(error), 'error')); });
190:  el<HTMLSelectElement>('#line-mode').addEventListener('change', (event) => { mode = (event.currentTarget as HTMLSelectElement).value as 'static' | 'animated'; const file = el<HTMLInputElement>('#line-file'); file.multiple = mode === 'animated'; el<HTMLElement>('#line-static-controls').hidden = mode === 'animated'; el<HTMLElement>('#line-animated-controls').hidden = mode !== 'animated'; updateStatus(mode === 'animated' ? 'Animated mode prepares and validates frames locally; APNG export is partial.' : 'Static mode supports sheet split and image sets locally.'); });
191:  el<HTMLButtonElement>('#line-split').addEventListener('click', performSplit); el<HTMLButtonElement>('#line-quick-split').addEventListener('click', () => el<HTMLButtonElement>('#line-split').click()); el<HTMLButtonElement>('#line-advanced-grid').addEventListener('click', () => { el<HTMLElement>('#line-static-controls').hidden = false; el<HTMLElement>('#line-grid-overlay').scrollIntoView({ behavior: 'smooth', block: 'center' }); }); el<HTMLSelectElement>('#line-rows').addEventListener('change', () => { updateSplitSummary(); if (sourceCanvas) { grid = createGrid(sourceCanvas.width, sourceCanvas.height, Number(el<HTMLSelectElement>('#line-rows').value), Number(el<HTMLSelectElement>('#line-columns').value)); splitState = 'grid-ready'; stickers.forEach(cleanupSticker); stickers = []; renderGridOverlay(); renderWorkspace(); } }); el<HTMLSelectElement>('#line-columns').addEventListener('change', () => { updateSplitSummary(); if (sourceCanvas) { grid = createGrid(sourceCanvas.width, sourceCanvas.height, Number(el<HTMLSelectElement>('#line-rows').value), Number(el<HTMLSelectElement>('#line-columns').value)); splitState = 'grid-ready'; stickers.forEach(cleanupSticker); stickers = []; renderGridOverlay(); renderWorkspace(); } });
192:  el<HTMLButtonElement>('#line-equalize').addEventListener('click', () => { if (!grid || !sourceCanvas) return; grid = createGrid(sourceCanvas.width, sourceCanvas.height, grid.rows, grid.columns); splitState = 'grid-ready'; stickers.forEach(cleanupSticker); stickers = []; selectedIndex = 0; renderGridOverlay(); renderWorkspace(); updateStatus('Grid ถูกปรับแล้ว · ตรวจ overlay แล้วกด Quick Split / Grid updated · review then Quick Split', 'success'); });
193:  el<HTMLButtonElement>('#line-clean').addEventListener('click', () => { const scope = el<HTMLSelectElement>('#line-scope').value as 'current' | 'selected' | 'all'; applyTo(scope, cleanItem); updateStatus('Background cleanup applied locally / ลบพื้นหลังในอุปกรณ์แล้ว', 'success'); }); el<HTMLButtonElement>('#line-clean-all').addEventListener('click', () => void runBatch('ลบพื้นหลังทั้งชุด', (item) => { pushHistory(item); cleanItem(item); })); el<HTMLButtonElement>('#line-autofit-all').addEventListener('click', () => void runBatch('จัดภาพให้อยู่กลางทั้งหมด', (item) => { pushHistory(item); autoFitItem(item); })); el<HTMLButtonElement>('#line-border-all').addEventListener('click', () => void runBatch('ใส่ขอบขาวทั้งชุด', (item) => { pushHistory(item); applyWhiteBorder(item); })); el<HTMLButtonElement>('#line-finish-all').addEventListener('click', () => void recommendedFinish());
194:  el<HTMLButtonElement>('#line-autofit').addEventListener('click', () => { const scope = el<HTMLSelectElement>('#line-scope').value as 'current' | 'selected' | 'all'; applyTo(scope, autoFitItem); updateStatus('Auto Fit applied / จัดวางอัตโนมัติแล้ว', 'success'); });
195:  el<HTMLButtonElement>('#line-apply-style').addEventListener('click', () => { const scope = el<HTMLSelectElement>('#line-scope').value as 'current' | 'selected' | 'all'; const style = defaultStyle(); style.keyColor = el<HTMLInputElement>('#line-key-color').value; style.tolerance = Number(el<HTMLInputElement>('#line-tolerance').value); style.feather = Number(el<HTMLInputElement>('#line-feather').value); style.strokeColor = el<HTMLInputElement>('#line-stroke-color').value; style.strokeWidth = Number(el<HTMLInputElement>('#line-stroke-width').value); style.showSafeMargin = el<HTMLInputElement>('#line-safe-margin').checked; applyTo(scope, (item) => { item.style = cloneStyle(style); }); updateStatus('Style applied / ใช้ style แล้ว', 'success'); });
196:  el<HTMLButtonElement>('#line-rotate').addEventListener('click', () => { const item = stickers[selectedIndex]; if (!item) return; pushHistory(item); item.transform.rotation = (item.transform.rotation + 90) % 360; renderWorkspace(); });
197:  el<HTMLButtonElement>('#line-flip').addEventListener('click', () => { const item = stickers[selectedIndex]; if (!item) return; pushHistory(item); item.transform.flipX = !item.transform.flipX; renderWorkspace(); });
198:  el<HTMLSelectElement>('#line-fit').addEventListener('change', (event) => { const item = stickers[selectedIndex]; if (!item) return; pushHistory(item); item.transform.fit = (event.currentTarget as HTMLSelectElement).value as StickerTransform['fit']; renderWorkspace(); });
199:  el<HTMLInputElement>('#line-zoom').addEventListener('input', (event) => { const item = stickers[selectedIndex]; if (!item) return; item.transform.scale = Number((event.currentTarget as HTMLInputElement).value) / 100; renderWorkspace(); });
200:  el<HTMLButtonElement>('#line-undo').addEventListener('click', undoCurrent); el<HTMLButtonElement>('#line-reset-current').addEventListener('click', resetCurrent); el<HTMLButtonElement>('#line-up').addEventListener('click', () => reorder(-1)); el<HTMLButtonElement>('#line-down').addEventListener('click', () => reorder(1)); el<HTMLButtonElement>('#line-delete').addEventListener('click', deleteCurrent); el<HTMLButtonElement>('#line-duplicate').addEventListener('click', duplicateCurrent); el<HTMLButtonElement>('#line-export-zip').addEventListener('click', () => void exportSet().catch((error) => updateStatus(getErrorMessage(error), 'error'))); el<HTMLButtonElement>('#line-download-current').addEventListener('click', downloadCurrent); el<HTMLButtonElement>('#line-play').addEventListener('click', playAnimation); el<HTMLButtonElement>('#line-stop').addEventListener('click', stopAnimation); el<HTMLButtonElement>('#line-validate-animation').addEventListener('click', () => { const checks = validateAnimatedFrames(stickers.map((item) => ({ width: item.canvas.width, height: item.canvas.height, bytes: item.blob?.size || 0, hasAlpha: inspectSticker(item.canvas.getContext('2d')!.getImageData(0, 0, item.canvas.width, item.canvas.height), ANIMATED_STICKER_PRESET).hasTransparency })), ANIMATED_STICKER_PRESET, frameDurations.reduce((sum, value) => sum + value, 0), Number(el<HTMLInputElement>('#line-loops').value)); updateStatus(checks.map((check) => `${check.level} ${check.label}: ${check.detail}`).join(' · '), checks.some((check) => check.level === 'FAIL') ? 'error' : 'success'); }); el<HTMLButtonElement>('#line-prompt-generate').addEventListener('click', buildPromptUi); el<HTMLButtonElement>('#line-prompt-copy').addEventListener('click', async () => { const copied = await copyPrompt(); updateStatus(copied ? 'คัดลอก Prompt แล้ว / Prompt copied' : 'คัดลอกไม่สำเร็จ กรุณาเลือกและคัดลอกข้อความด้วยตนเอง', copied ? 'success' : 'error'); }); el<HTMLButtonElement>('#line-prompt-handoff').addEventListener('click', async () => { buildPromptUi(); const copied = await copyPrompt(); if (copied) openChatGPT(); else updateStatus('คัดลอกไม่สำเร็จ กรุณากดคัดลอก Prompt ก่อน', 'error'); }); el<HTMLButtonElement>('#line-prompt-open').addEventListener('click', openChatGPT); el<HTMLSelectElement>('#line-preset-grid').addEventListener('change', (event) => { const value = (event.currentTarget as HTMLSelectElement).value; const preset = value === '3x3' ? [3, 3, 9] : value === '4x4' ? [4, 4, 16] : value === '4x5' ? [4, 5, 20] : undefined; if (preset) { el<HTMLSelectElement>('#line-rows').value = String(preset[0]); el<HTMLSelectElement>('#line-columns').value = String(preset[1]); el<HTMLInputElement>('#line-prompt-count').value = String(preset[2]); buildPromptUi(); } persistDraft(); });
201:  el<HTMLButtonElement>('#line-new-set').addEventListener('click', createNewSet); el<HTMLButtonElement>('#line-handoff-copy').addEventListener('click', async () => { const copied = await copyPrompt(); updateStatus(copied ? 'คัดลอก Prompt ใหม่แล้ว / Prompt copied again' : 'คัดลอกไม่สำเร็จ', copied ? 'success' : 'error'); }); el<HTMLButtonElement>('#line-review-refresh').addEventListener('click', refreshReview); el<HTMLButtonElement>('#line-fix-next').addEventListener('click', () => { const index = stickers.findIndex((item) => item.quality?.level !== 'PASS'); if (index >= 0) { selectedIndex = index; currentStep = 'edit'; renderWorkspace(); el<HTMLElement>('[data-line-section="edit"]').scrollIntoView({ behavior: 'smooth', block: 'start' }); } else updateStatus('No failing or warning sticker found / ไม่พบปัญหาที่ต้องแก้', 'success'); });
202:  panel!.querySelectorAll<HTMLButtonElement>('[data-line-step]').forEach((button) => button.addEventListener('click', () => { currentStep = button.dataset.lineStep || currentStep; const step = panel!.querySelector<HTMLElement>(`[data-line-section="${currentStep}"]`); step?.scrollIntoView({ behavior: 'smooth', block: 'start' }); renderWorkspace(); }));
209:    panel.innerHTML = `<div class="utility-panel__header line-studio-header"><div><p class="eyebrow">Local Sticker Workspace</p><h2>LINE Sticker Studio</h2><p class="helper-text">Split, clean, edit, inspect and export locally. No upload, no AI server, no account.</p></div><span class="privacy-badge">Local-only / ไม่อัปโหลด</span></div><div class="line-mode-row"><label class="field"><span>Mode</span><select id="line-mode"><option value="static">Static stickers</option><option value="animated">Animated frame preparation</option></select></label><p class="helper-text">Technical preset only; LINE human review is not automatically verified.</p></div><nav class="line-steps" aria-label="Sticker workflow"><button type="button" data-line-step="prompt">1 สร้าง Prompt</button><button type="button" data-line-step="handoff">2 สร้างภาพด้วย AI</button><button type="button" data-line-step="upload">3 อัปโหลดและตัดภาพ</button><button type="button" data-line-step="edit">4 ลบพื้นหลัง/ตกแต่ง</button><button type="button" data-line-step="review">5 ตรวจและแก้</button><button type="button" data-line-step="export">6 ดาวน์โหลด</button></nav><section data-line-section="handoff" class="line-section line-handoff-card"><h3>ได้ภาพ Sticker Sheet แล้วหรือยัง?</h3><p>กลับมาที่นี่หลังสร้างภาพกับ provider ภายนอก แล้วอัปโหลดเพื่อเริ่มตัดภาพ</p><button type="button" class="button button--primary" data-line-step="upload">อัปโหลด Sticker Sheet</button><button type="button" class="button button--secondary" data-line-step="prompt">เปิด Prompt อีกครั้ง</button><button type="button" class="button button--secondary" id="line-handoff-copy">คัดลอก Prompt ใหม่</button></section><section data-line-section="upload" class="line-section"><h3>Upload / นำเข้า</h3><label class="file-drop" for="line-file"><strong>Choose PNG, JPEG or WebP</strong><span>Sticker sheet, multiple images, or animation frames</span><input id="line-file" type="file" accept="image/png,image/jpeg,image/webp" multiple /></label><div class="line-privacy-note">ภาพอยู่ใน browser เท่านั้น / Images stay in this browser. Prompt Studio does not call an AI API.</div><div class="line-source-preview-card"><strong>Source preview / ภาพต้นฉบับ</strong><span id="line-source-meta">ยังไม่มี source</span><canvas id="line-source-preview" aria-label="Original sticker sheet preview"></canvas></div><div class="line-quick-split-card"><strong id="line-split-summary">Suggested Grid: 2×4 · 8 ภาพ</strong><p>ตรวจ overlay และปรับเส้นตัดได้ภายหลัง</p><button id="line-quick-split" class="button button--primary" type="button">ตัด 8 ภาพ</button><button id="line-advanced-grid" class="button button--secondary" type="button">ปรับเส้นตัด</button></div><div id="line-static-controls"><div class="line-control-grid"><label class="field"><span>Rows</span><select id="line-rows"><option>1</option><option selected>2</option><option>3</option><option>4</option></select></label><label class="field"><span>Columns</span><select id="line-columns"><option>1</option><option>2</option><option selected>4</option><option>3</option><option>4</option></select></label><button id="line-split" class="button" type="button">Apply grid</button><button id="line-equalize" class="button" type="button">Equalize grid</button></div><div id="line-grid-overlay" class="line-grid-overlay" aria-label="Adjustable grid boundaries"></div></div><div id="line-animated-controls" hidden><label class="field"><span>Loop count</span><input id="line-loops" type="number" min="1" max="4" value="1" /></label><p id="line-animated-summary" class="helper-text">Frames 0 · Animation Duration 0s</p><button id="line-validate-animation" class="button" type="button">Validate frames</button><button id="line-play" class="button" type="button">Play</button><button id="line-stop" class="button" type="button">Pause</button></div></section><section data-line-section="split" class="line-section"><h3>Sticker navigator / รายการ stickers</h3><div id="line-thumbnails" class="line-thumbnail-rail"></div><div class="line-toolbar"><button id="line-up" class="button" type="button">Move up</button><button id="line-down" class="button" type="button">Move down</button><button id="line-duplicate" class="button" type="button">Duplicate</button><button id="line-delete" class="button" type="button">Delete</button></div></section><section data-line-section="edit" class="line-section"><div class="line-editor-layout"><aside><h3>Sticker list</h3><div id="line-thumbnails-side" class="line-thumbnail-rail"></div></aside><div class="line-canvas-wrap"><h3 id="line-selected-title">No sticker selected</h3><div class="line-canvas-stage"><canvas id="line-canvas" width="320" height="320" aria-label="Sticker preview"></canvas></div><p class="helper-text">Checkerboard shows transparency. Dashed line shows the technical safe margin.</p></div><aside class="line-inspector-panel"><h3>Inspector / ตรวจคุณภาพ</h3><div id="line-inspector">Upload images to begin</div></aside></div><div class="line-control-grid"><label class="field"><span>Zoom</span><input id="line-zoom" type="range" min="50" max="180" value="100" /></label><label class="field"><span>Fit</span><select id="line-fit"><option value="contain">Contain</option><option value="cover">Cover</option></select></label><button id="line-rotate" class="button" type="button">Rotate</button><button id="line-flip" class="button" type="button">Flip horizontal</button><button id="line-undo" class="button" type="button">Undo</button></div><div class="line-batch-actions"><strong>จัดการทั้งชุด</strong><button id="line-clean-all" class="button" type="button">ลบพื้นหลังทั้งชุด</button><button id="line-autofit-all" class="button" type="button">จัดภาพให้อยู่กลางทั้งหมด</button><button id="line-border-all" class="button" type="button">ใส่ขอบขาวทั้งชุด</button><button id="line-finish-all" class="button button--primary" type="button">จัดชุดอัตโนมัติ</button><output id="line-batch-status" aria-live="polite"></output></div><div class="line-style-controls"><label class="field"><span>Key color</span><input id="line-key-color" type="color" value="#ffffff" /></label><label class="field"><span>Tolerance</span><input id="line-tolerance" type="range" min="0" max="160" value="35" /></label><label class="field"><span>Feather</span><input id="line-feather" type="range" min="1" max="40" value="10" /></label><label class="field"><span>Stroke color</span><input id="line-stroke-color" type="color" value="#ffffff" /></label><label class="field"><span>Stroke width</span><input id="line-stroke-width" type="range" min="0" max="24" value="0" /></label><label class="field"><span>Safe margin <input id="line-safe-margin" type="checkbox" checked /></span></label><label class="field"><span>Apply scope</span><select id="line-scope"><option value="current">Current</option><option value="all">All</option></select></label><button id="line-clean" class="button" type="button">Clean white background</button><button id="line-autofit" class="button" type="button">Auto Fit</button><button id="line-apply-style" class="button" type="button">Apply style</button><button id="line-reset-current" class="button button--secondary" type="button">Reset current</button></div></section><section data-line-section="review" class="line-section"><h3>Review / ตรวจทั้งชุด</h3><div class="line-review-summary"><p>Technical checks are local and do not guarantee LINE content approval.</p><div id="line-review-report"></div></div><button class="button" type="button" id="line-review-refresh">Refresh validation</button><button class="button" type="button" id="line-fix-next">Fix next issue</button></section><section data-line-section="export" class="line-section"><h3>ดาวน์โหลดชุด Sticker</h3><div class="line-export-grid"><button id="line-export-zip" class="button button--primary" type="button">ดาวน์โหลด ZIP</button><button id="line-download-current" class="button" type="button">ดาวน์โหลดภาพปัจจุบัน</button><button id="line-new-set" class="button button--secondary" type="button">สร้างชุดใหม่</button></div><div id="line-export-summary" class="line-export-summary"><span>หลัง export จะแสดงจำนวนไฟล์ ขนาด ZIP และ validation status ที่นี่</span></div><p class="helper-text">ZIP contains stickers/01.png, main.png, tab.png, and validation-report.json. APNG export is partial and not advertised as ready.</p></section><section data-line-section="prompt" class="line-section line-prompt"><h3>Prompt Studio</h3><p class="helper-text">สร้าง prompt ให้ copy ไปใช้กับ image generator เอง; ไม่มีการเรียก AI API จากเครื่องมือนี้</p><div class="line-control-grid"><label class="field"><span>Character</span><input id="line-prompt-character" placeholder="friendly cat" /></label><label class="field"><span>Style</span><input id="line-prompt-style" placeholder="clean cartoon" /></label><label class="field"><span>Outfit</span><input id="line-prompt-outfit" placeholder="blue hoodie" /></label><label class="field"><span>Expressions</span><input id="line-prompt-expressions" placeholder="happy, sorry, thanks" /></label><label class="field"><span>Phrases</span><input id="line-prompt-phrases" placeholder="สวัสดี, ขอบคุณ, โอเค" /></label><label class="field"><span>Language</span><input id="line-prompt-language" value="Thai" /></label><label class="field"><span>Count / จำนวน</span><input id="line-prompt-count" type="number" min="1" max="40" value="8" /></label><label class="field"><span>Grid preset</span><select id="line-preset-grid"><option value="3x3">3×3 · 9</option><option value="4x4" selected>4×4 · 16</option><option value="4x5">4×5 · 20</option><option value="custom">Custom</option></select></label><label class="field"><span>Background</span><input id="line-prompt-background" value="transparent" /></label></div><fieldset><legend>Consistency helpers</legend><label><input type="checkbox" data-consistency value="same character" checked /> same character</label><label><input type="checkbox" data-consistency value="same hairstyle" checked /> same hairstyle</label><label><input type="checkbox" data-consistency value="same colors" checked /> same colors</label><label><input type="checkbox" data-consistency value="same rendering style" checked /> same rendering style</label><label><input type="checkbox" data-consistency value="no extra characters or text" checked /> no extra characters or text</label></fieldset><button id="line-prompt-generate" class="button" type="button">สร้าง Prompt</button><button id="line-prompt-copy" class="button button--secondary" type="button">คัดลอก Prompt</button><button id="line-prompt-handoff" class="button button--primary" type="button">คัดลอก Prompt + เปิด ChatGPT</button><button id="line-prompt-open" class="button button--secondary" type="button">เปิด ChatGPT</button><p id="line-grid-warning" class="line-grid-warning" aria-live="polite"></p><p class="line-external-note">Utility Hub จะไม่ส่ง Prompt หรือภาพไป ChatGPT เอง การเปิด ChatGPT เป็นการเปิดเว็บไซต์ภายนอกตามคำสั่งของคุณ</p><label class="field"><span>Thai prompt</span><textarea id="line-prompt-th" rows="4"></textarea></label><label class="field"><span>English prompt</span><textarea id="line-prompt-en" rows="4"></textarea></label><p id="line-status" class="tool-status" aria-live="polite">Choose images to begin / เลือกรูปเพื่อเริ่มต้น</p></section>`;
215:export const { mount, unmount } = tool;
216:export { metadata };
217:export default tool;
